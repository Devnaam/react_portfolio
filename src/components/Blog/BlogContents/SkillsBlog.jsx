import React from "react";

function SkillsBlog({ theme }) {
  return (
    <div className={`text-lg ${theme ? "text-gray-300" : "text-gray-700"}`}>
      <p>Skills blog content coming soon...</p>
    </div>
  );
}

export default SkillsBlog;
